# API base URL (required — no code fallbacks)
# Local dev: use /api with VITE_API_PROXY_TARGET
VITE_API_URL=/api
VITE_API_PROXY_TARGET=http://127.0.0.1:3001

# Production / remote API (no proxy target needed)
# VITE_API_URL=https://yarofragrance.com/api


# Public R2 CDN for category banners (no secrets — bucket must allow r2.dev access)
VITE_R2_PUBLIC_URL=https://pub-180262caf54b4344a756b2202e7d0335.r2.dev
